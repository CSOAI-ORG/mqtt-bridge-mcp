# mqtt-bridge-mcp

mcp-name: io.github.CSOAI-ORG/mqtt-bridge-mcp

Part of the **CSOAI Layer-0 legacy-bridge family**. Bridge MQTT / IoT messaging to ONE OS — parse, map, govern device security (IEC 62443 / NIS2).

**Tools:** parse_mqtt · map_to_modern · govern_iot

```bash
pip install -e .
python server.py
```
